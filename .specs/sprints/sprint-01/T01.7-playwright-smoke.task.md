---
id: T01.7
title: Define first Playwright CLI smoke coverage
sprint: sprint-01
owner: us4-core
status: done
---

# T01.7 - Define first Playwright CLI smoke coverage

## Context

Sprint 01 needs a first E2E contract, even before real inference exists. The smoke should target CLI metadata and probe flows first.

## Acceptance criteria

- [x] first smoke target is `--version`
- [x] second smoke target is `--probe`
- [x] evidence expectations are explicit

## Evidence

- `.specs/runtime/PLAYWRIGHT-SMOKE.md` locks `--version` as target 1, `--probe` as target 2, and the additional `--mode auto --json` coverage.
- `tests/e2e/us4-cli.spec.ts` materializes both targets and the native variant.
- `tests/e2e/README.md` lists the exact evidence artifacts the DoD gate enforces.
- `.github/workflows/dod.yml` blocks merge if Playwright evidence (`playwright-report/index.html`, `test-results/results.{json,xml}`, `trace.zip`) is missing.

## Validation

- sprint doc and workflow doc align on smoke scope
