---
id: T01.2
title: Define us4-cli version and probe contract
sprint: sprint-01
owner: us4-core
status: done
---

# T01.2 - Define us4-cli version and probe contract

## Context

The sprint requires CLI smoke coverage, but that only works if `us4-cli` output and intent are specified first.

## Acceptance criteria

- [x] `--version` output contract is documented
- [x] `--probe` output contract is documented
- [x] JSON versus text expectations are stated

## Evidence

- `.specs/runtime/CLI-CONTRACT.md` defines the full output contract for `us4-cli`.
- Smoke E2E `tests/e2e/us4-cli.spec.ts` validates text and JSON forms for `--version`, `--probe`, `--mode auto`, and the native runtime variant.
- Smoke JS `test/cli.smoke.test.js` validates the same contract on the Node starter.

## Validation

- docs consistency
- Sprint 01 smoke path aligns with CLI contract
