---
id: T07.6
title: Add llama benchmark evidence
sprint: sprint-07
owner: us4-core
status: done
---

# T07.6 - Add llama benchmark evidence

## Context

Sprint 07 needs more than functional contracts. We still need benchmark and
correctness evidence showing how the Llama path behaves across available
backends and fallbacks.

## Acceptance criteria

- [x] Bench output includes at least one Llama fixture or toy asset path
- [x] Evidence records requested backend, observed backend, mode, and text fingerprint
- [x] Correctness delta placeholder is explicit until an external reference path exists

## Current progress

- `runtime/benchmarks/dense_baseline.cpp` now emits two Llama cases:
  `llama-fixture/metal-requested` and `llama-gguf/neon-requested`
- Each case records `asset_format`, `asset_path`, `requested_backend`,
  `observed_backend`, `backend_reason`, `mode`, `generated_tokens`,
  `elapsed_ms`, and `text_fingerprint`
- Correctness remains explicitly marked as
  `pending_external_reference` until the HF reference path exists

## Validation

- benchmark evidence
- native CLI regression
- docs consistency in sprint notes

## Links

- `.specs/sprints/sprint-07/SPRINT.md`
- `runtime/benchmarks/dense_baseline.cpp`
